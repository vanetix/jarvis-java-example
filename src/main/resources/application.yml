spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/jarvis_dev
    username: postgres
    password: postgres

  jpa:
    show-sql: true

endpoint:
  metrics:
    enabled: true
